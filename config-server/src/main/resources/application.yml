server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/arodriguezcu/Proyecto3-everis.git
          searchPaths: config-data
          default-label: main
